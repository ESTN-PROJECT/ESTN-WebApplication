{% extends 'base.html.twig' %}

{% block title %}Forum{% endblock %}

{% block body %}
    <h1>Forum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forum.id }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ forum.sujet }}</td>
            </tr>
            <tr>
                <th>Archive</th>
                <td>{{ forum.archive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forum2_index') }}">back to list</a>

    <a href="{{ path('app_forum2_edit', {'id': forum.id}) }}">edit</a>

    {{ include('forum2/_delete_form.html.twig') }}
{% endblock %}
