{% extends 'base.html.twig' %}


{% block title %}Hello EvenementController!{% endblock %}

{% block body %}
    <!-- Table with stripped rows -->
    <table class="table table-striped">
        <thead>
        <tr>
            <th>id event</th>
            <th>event title</th>
            <th>event description</th>
            <th>event date</th>
            <th>Modification</th>
            <th>Suppression</th>
        </tr>
        </thead>
        <tbody>
        {% for evenement in e %}
            <tr>
                <td>{{ evenement.idEvent }}</td>
                <td>{{ evenement.titreEvent }}</td>
                <td>{{ evenement.descriptionEvent }}</td>
                <td>{{ evenement.dateEvent.format('d/m/Y') }}</td>
                <td>
                    <a class="btn btn-success" href="{{ path('modifEvenement',{'id':evenement.idEvent}) }}">Modifier</a>
                </td>
                <td>
                    <a class="btn btn-danger" href="{{ path('supp_evenement',{'id':evenement.idEvent}) }}">Supprimer</a>
                </td>
            </tr>

        {% endfor %}

        </tbody>
    </table>
    <!-- End small tables -->
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a
                            href="{{ 'C:/xampp/htdocs/SymfonyProjects/ESTN/src/Controller/EvenementController.php'|file_link(0) }}">src/Controller/EvenementController.php</a></code>
            </li>
            <li>Your template at <code><a
                            href="{{ 'C:/xampp/htdocs/SymfonyProjects/ESTN/templates/evenement/index.html.twig'|file_link(0) }}">templates/evenement/index.html.twig</a></code>
            </li>
        </ul>
    </div>
{% endblock %}
