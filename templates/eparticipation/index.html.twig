{#{% extends 'baseBack.html.twig' %}


{% block title %}Hello EparticipationController!{% endblock %}


{% block body %}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script>
    $(document).ready(function(){
        $("#myInput").on("keyup", function() {
            var value = $(this).val().toLowerCase();
            $("#myTable tr").filter(function() {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });
</script>
    <!-- Table with stripped rows -->

    <table id="myTable" class="table table-striped">
        <thead>
        <tr>

            <th>Title</th>
            <th>Description</th>
            <th>Date</th>
            <th>Participate</th>


        </tr>
        </thead>
        <tbody>
        <input id="myInput" type="search" placeholder="Search" aria-label="Search"/> <img src="https://img.icons8.com/nolan/344/google-web-search.png
" style="width:50px;height:50px;"/></p>
        {% for evenement in e %}
            <tr>

                <td>{{ evenement.titreEvent }}</td>
                <td>{{ evenement.descriptionEvent }}</td>
                <td>{{ evenement.dateEvent.format('d/m/Y') }}</td>
                <td>
                    <a class="btn btn-success" href="{{ path('prtevenement',{'id':evenement.idEvent}) }}">Participate</a>
                </td>


            </tr>

        {% endfor %}

        </tbody>
    </table>
    <!-- End small tables -->
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>


{% endblock %}

#}
{% extends 'baseBack.html.twig' %}

{% block title %} Events Participation !{% endblock %}

{% block body %}

    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a href="{{ 'C:/xampp/htdocs/SymfonyProjects/ESTN/src/Controller/FrontController.php'|file_link(0) }}">src/Controller/FrontController.php</a></code></li>
            <li>Your template at <code><a href="{{ 'C:/xampp/htdocs/SymfonyProjects/ESTN/templates/front/index.html.twig'|file_link(0) }}">templates/front/index.html.twig</a></code></li>
        </ul>

    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $(document).ready(function(){
            $("#myInput").on("keyup", function() {
                var value = $(this).val().toLowerCase();
                $("#myTable tr").filter(function() {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                });
            });
        });
    </script>
    <!-- Table with stripped rows -->

    <table id="myTable" class="table table-striped">
        <thead>
        <tr>

            <th>Title</th>
            <th>Description</th>
            <th>Date</th>
            <th>Participate</th>


        </tr>
        </thead>
        <tbody>
        <input id="myInput" type="search" placeholder="Search" aria-label="Search"/> <img src="https://img.icons8.com/nolan/344/google-web-search.png
" style="width:50px;height:50px;"/></p>
        {% for evenement in e %}
            <tr>

                <td>{{ evenement.titreEvent }}</td>
                <td>{{ evenement.descriptionEvent }}</td>
                <td>{{ evenement.dateEvent.format('d/m/Y') }}</td>
                <td>
                    <a class="btn btn-success" href="{{ path('prtevenement',{'id':evenement.idEvent}) }}">Participate</a>
                </td>


            </tr>

        {% endfor %}

        </tbody>
    </table>
    <!-- End small tables -->
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>


{% endblock %}
